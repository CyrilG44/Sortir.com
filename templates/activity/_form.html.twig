
    {{ form_start(form) }}

    <!-- Formulaire avec les styles appliqués -->
    {% for child in form %}
        <div class="form-group">
            {{ form_label(child, null, {'label_attr': {'class': 'form-label'}}) }}
            {{ form_widget(child, {'attr': {'class': 'form-input'}}) }}
            {{ form_errors(child) }}
        </div>
    {% endfor %}

    <div class="flex justify-center">
        <button class="form-button ">{{ button_label|default('Enregistrer') }}</button>
    </div>

    {% if not activity.id %}
        <input type="submit" formaction="{{ path('app_activity_create', {'autoPublish' : 'true'}) }}" value="Publier">
    {%  endif %}
{{ form_end(form) }}
